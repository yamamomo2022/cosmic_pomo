/// Represents the different modes of the Pomodoro timer
///
/// [workMode] - The focused work session
/// [breakMode] - The rest period between work sessions
enum PomodoroMode {
  /// Work session mode - typically 25 minutes
  workMode,

  /// Break session mode - typically 5 minutes (or 3 minutes in this app)
  breakMode,
}
